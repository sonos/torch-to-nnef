# also support baddbmm (aka batch version)
fragment addmm(
    input: tensor<scalar>,
    batch1: tensor<scalar>,
    batch2: tensor<scalar>,
    alpha: scalar,
    beta: scalar
) -> ( y: tensor<scalar> ) {
    y = beta * input + alpha * matmul(batch1, batch2);
}

# `addcdiv(self, t1, t2, value=1.0) = self + value * (t1 / t2)`.
fragment addcdiv(
    input: tensor<scalar>,
    a:     tensor<scalar>,
    b:     tensor<scalar>,
    value: scalar = 1.0
) -> ( output: tensor<scalar> )
{
    output = input + value * (a / b);
}

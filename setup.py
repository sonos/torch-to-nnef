#!/usr/bin/env python

# This is a shim to hopefully allow Github to detect the package, build is done with uv

import setuptools

if __name__ == "__main__":
    setuptools.setup(name="torch_to_nnef")

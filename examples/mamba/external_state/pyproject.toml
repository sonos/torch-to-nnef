[project]
name = "t2n-example-mamba-external-state"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "torch_to_nnef",
    "torch==2.12.0",
    "transformers==5.9.0",
    "huggingface_hub==1.17.0",
]

[tool.uv]
package = false

[tool.uv.sources]
torch_to_nnef = { path = "../../../", editable = true }

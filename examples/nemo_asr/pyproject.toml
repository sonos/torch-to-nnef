[project]
name = "t2n-example-nemo-asr"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "torch_to_nnef[nemo-tract]",
    # Pin torch to match torchaudio (caps at 2.11, no 2.12 yet): without this
    # the resolver picks torch 2.12 + torchaudio 2.11, which t2n rejects as a
    # mismatched pair.
    "torch==2.11.*",
    "torchaudio==2.11.*",
]

[tool.uv]
package = false

[tool.uv.sources]
torch_to_nnef = { path = "../../", editable = true }

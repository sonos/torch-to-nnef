-e ../../..
torch==2.11.*
torchaudio==2.11.*
# Upstream DeepFilterNet from PyPI -- provides `df.enhance.init_df()` and the
# pretrained DFN3 checkpoint. grazder's `torch_df_streaming_minimal.py` is
# *not* on PyPI; bootstrap.sh clones their fork and adds it to sys.path
# (see export.py / export_stft_variant.py).
deepfilternet>=0.5,<0.6
soundfile>=0.12
onnx>=1.14
onnxruntime>=1.17
onnxsim>=0.4
loguru>=0.5

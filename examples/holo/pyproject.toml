[project]
name = "t2n-example-holo"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "torch_to_nnef[llm]",
    "torch==2.13.0",
    "transformers==5.9.0",
    "huggingface_hub==1.17.0",
    "pillow",
]

[tool.uv]
package = false

[tool.uv.sources]
torch_to_nnef = { path = "../../", editable = true }
